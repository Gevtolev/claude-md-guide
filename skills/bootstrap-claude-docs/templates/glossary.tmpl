# {{PROJECT_NAME}} — 术语表

> 统一本项目的领域语言（ubiquitous language）。命名、文档、代码、和 AI 对话都用这里的词。
> 这是一份**术语表，仅此而已**——不写实现细节、不当 spec、不当草稿本。

## 术语

**{{TERM}}**：一句话定义（定义它**是什么**，不是它做什么）。
_Avoid_：被否决的同义词

<!-- 示例：
**Order**：客户购买一个或多个商品的请求。
_Avoid_：Purchase, Transaction

**Invoice**：交付后向客户发出的付款请求。
_Avoid_：Bill, Payment request
-->

## 写作规范

- **只收本项目特有的术语**——通用编程概念（超时、错误类型、工具函数）不收，哪怕项目里大量用到。判据："这是本项目独有的概念，还是通用编程概念？"只有前者属于这里
- **opinionated**：一个概念只留一个最佳术语，其余全列进 `_Avoid_`。这正是语言压缩黑话的方式
- **定义紧凑**：一两句话，定义它**是什么**而非**做什么**
- **就地修订**：术语含义变了直接改这里，不留 stale 条目；术语之间用术语表自己的词
- **绑定代码**：术语定型后，变量 / 函数 / 文件命名应与之一致——这是术语表最大的回报（命名一致、AI 导航更准）
- 自然形成簇时用二级标题分组，否则平铺

## lazy 创建

本文件**按需创建**：第一个值得统一的术语出现时再建，不预先建空表。没有领域黑话的项目（如纯工具脚本）可以一直没有这份文件。
