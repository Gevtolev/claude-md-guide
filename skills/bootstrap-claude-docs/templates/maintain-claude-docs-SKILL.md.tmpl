---
name: maintain-claude-docs
description: |
  On-demand / periodic documentation audit for this project. Use when the user explicitly
  asks to update or audit docs ("更新文档" / "文档体检"), or at a milestone (a phase wraps
  up, before a release) — NOT after every change. {{TYPE_SPECIFIC_TRIGGER_SENTENCE}}
  Keeps decisions/ADRs and README indexes honest without over-documenting; only record
  reversal-resistant, non-obvious decisions.
---

# Maintain Claude Docs

确保本项目的文档体系与代码改动同步演进。

## When to Use

- 用户主动要求「更新文档 / 文档体检」
- 阶段性节点（功能收尾、发版前）做一次文档体检
- 产生了**难逆转 + 非显而易见**的关键决策（即时记一条 ADR）

**不要**在每次普通改动后都跑——日常改动只走 CLAUDE.md 的精简改动自查即可。

## When to Skip

- 仅修改注释、格式化、变量名
- 仅修文档拼写错误
- 修复明确不影响行为的 bug

## Maintenance Checklist

完成本次改动后，逐项检查：

1. **是否产生「难逆转 + 非显而易见」的关键决策？** → 才在 `docs/decisions/` 新增 ADR（可写短，20–30 行够；标准格式 Status / Context / Decision / Consequences / Related，命名 `YYYY-MM-DD-<topic>.md`，矩阵补 1 行）。日常可逆改动不写。
2. **是否构成新功能或新模块？** → 顶层 `ARCHITECTURE.md` 对应章节是否需要更新？模块级深入是否在代码 docstring 写清？
3. **（可选）有 ADR 的 Context 段装不下的产品级"为什么"？** → 才考虑在 `docs/insights/` 写一份；不强制。
4. **是否增删了文档文件？** → 对应 `docs/<dir>/README.md` 索引同步了吗？
5. **是否发现了技术债务？** → 已添加到 `docs/exec-plans/tech-debt-tracker.md` 了吗？
6. **是否在做中大型功能？** → 已起 `docs/exec-plans/active/<topic>.md` 了吗？
7. **是否完成了 exec-plan？** → 已移到 `docs/exec-plans/completed/` 了吗？
8. **季度审计**：CLAUDE.md 中是否有因模型/工具演进而过时的规则？每 3 个月回顾一次，移除已不需要的约束。

## How to Apply

针对清单中需要补做的项，提示用户并按序补齐。每补完一项标注 ✅ 给用户看。

## Project-Specific Hints

{{PROJECT_SPECIFIC_HINTS}}
<!-- TODO: bootstrap 时根据项目类型填充。例：
- Web 应用：API 改动是否同步了 OpenAPI 文档？
- CLI 工具：新命令是否更新了 README 命令表？
- 库：API 改动是否标记了 SemVer 影响等级？
- Monorepo：新增子包后是否同步了子包 CLAUDE.md？
- 所有类型：.ignore 是否需要更新（新增了生成文件/大型资产）？
- 所有类型：只记「难逆转 + 非显而易见」的决策，ADR 可写短；日常可逆改动不写
-->
