# Exec Plans / 执行计划

中大型功能的执行计划，包含分阶段目标、进度状态和决策日志。

**AI 须知：**
- 新建执行计划放在 `active/`，完成后移至 `completed/`（移动本身就是收口）
- 纯调研放 `../research/`
- 修改或新增文件后更新下方索引
- **按依赖顺序推进**：每份计划用 `Blocked by` 标注前置依赖，形成 DAG，先做无依赖的
- **链接不复制**：计划保持紧凑（整体会被加载进上下文）；大块产出（调研/prototype/长设计）链接到 `../research/` 或单独文件，不在计划里复制；耐久的"为什么"进 `../decisions/` ADR

## 什么时候需要执行计划

- 涉及数据库 schema 变更
- 跨 3 个以上模块的功能
- 需要分阶段交付的中大型功能
- 重构或迁移类任务

## 执行计划模板

见 [exec-plan-template.md](./exec-plan-template.md)。

## 索引

### Active

| 文件 | 主题 | 状态 |
|------|------|------|
{{ACTIVE_PLANS_INDEX}}

### Completed

| 文件 | 主题 | 完成日期 |
|------|------|----------|
{{COMPLETED_PLANS_INDEX}}

## 技术债务

见 [tech-debt-tracker.md](./tech-debt-tracker.md)。
