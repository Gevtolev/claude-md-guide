# CLAUDE.md

{{PROJECT_NAME}} — {{ONE_LINE_DESCRIPTION}}

> 架构细节见 [ARCHITECTURE.md](./ARCHITECTURE.md)。本文件只含规则与流程。

## 开发规则

{{DEV_RULES_SECTION}}
<!-- TODO: 列出本项目特有的开发规则。例：提交前必须跑 X / UI 改动必须 Y / 新功能必须 Z -->

## 自检命令

{{SELF_CHECK_COMMANDS}}
<!-- TODO: 列出本地可跑的检查命令，按耗时由短到长排序 -->

## 改动自查

完成代码修改后，在提交前确认：

{{CHANGE_REVIEW_CHECKLIST}}
<!-- TODO: ≤4 条核心自查项 + 一行「软提示·按需」，别列长清单（越长越没人逐条走）。参考样例：
1. 改动涉及类型 / schema → 同步 `src/types/` / 迁移
2. 碰 secret → 客户端不泄露
3. 改动涉及核心计算 / 契约 → 有对应测试
4. 产生「难逆转 + 非显而易见」的关键决策 → 才记一份 `docs/decisions/` ADR（可写短）
> 软提示（按需、不强制写文档）：其余如 i18n 同步 / 依赖补 .env.example / 架构改动顺手更新 ARCHITECTURE.md
-->

## 工作流

{{WORKFLOW_SECTION}}
<!-- TODO: 描述本项目的标准开发工作流 -->

## 文档

- [ARCHITECTURE.md](./ARCHITECTURE.md) — 项目架构、目录结构、数据流
- `docs/decisions/` — **关键决策记录（ADR）**：架构与产品决策的追溯
- `docs/insights/` — 产品思考文档（**可选**；ADR 的 Context 段装不下时才写，不强制每改动补）
- `docs/research/` — 调研文档（技术方案、可行性分析）
- `docs/exec-plans/` — 执行计划（active / completed、技术债务）

**检索前先读对应目录的 README.md；增删文件后更新索引。**
