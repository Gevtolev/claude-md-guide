# Decisions / 关键决策记录（ADR）

{{PROJECT_NAME}} 的关键架构与产品决策记录，使用轻量 ADR（Architecture Decision Record）。

> 本目录是关键决策的**唯一来源**。每份 ADR 一个文件，按时间序号 + 主题 kebab-case 命名。
> **极简优先**：一份 ADR 默认就是标题 + 1-3 句话；段落按需，不为凑格式而填。

## 决策矩阵

| # | 日期 | 决策 | 状态 | 视角 |
|---|------|------|------|------|
{{DECISIONS_INDEX_ROWS}}
<!-- TODO: 每行格式：| **D#** | YYYY-MM-DD | [标题](./YYYY-MM-DD-topic.md) | Accepted | 产品/技术/架构/安全/组织 | -->

## 写作规范

- 文件命名：`YYYY-MM-DD-<topic>.md`（kebab-case + 决策日期前缀）
- **默认形态**：标题 + 1-3 句话（背景 / 决定 / 为什么）。价值在于记录"做了决定"和"为什么"，不在填满段落
- **可选段**（仅当有价值才加）：状态、候选方案、影响、关联
- **durability**：描述接口 / 契约 / 行为 / 理由——**不写文件路径、行号、代码片段**（会过时）。唯一例外是编码"决策本身"的 schema / 状态机片段
- 决策被推翻时：**新增**一份 ADR 标记原 ADR 为 `Superseded by D##`，**不修改原文**
- 跨 ADR 引用用相对链接：`[D##](./YYYY-MM-DD-topic.md)`
- 模板见 [`adr-template.md`](./adr-template.md)

## 何时新增 ADR

三个条件**同时成立**才写（缺一即跳过）：

1. **难逆转**——将来改变主意的代价不小
2. **没上下文会困惑**——未来的人看代码会问"为什么这么做"
3. **真有取舍**——确实有别的备选，你为具体理由选了这个

可逆就跳过（反正会改回来）；不意外就没人会问；没有备选就没什么可记的。

**什么算 ADR-worthy**（对照识别）：

- 架构形态（"用 monorepo"、"写模型事件溯源"）
- 跨上下文的集成模式（领域事件 vs 同步 HTTP）
- 带锁定的技术选型（数据库 / 消息总线 / 鉴权——换掉要花一个季度的那种）
- 边界与范围决定（"X 数据归 A 上下文所有，其他只引 ID"——显式的"不做"和"做"一样值钱）
- 反直觉的偏离（"用手写 SQL 而非 ORM，因为 X"——拦住下一个人去"修正"一个故意的设计）
- 代码里看不到的约束（合规、延迟 SLA）
- 非显而易见的否决（考虑过 GraphQL 选了 REST——否则半年后有人又提一遍）

不需要 ADR：日常代码风格、非关键依赖选择、可逆的小修改。

## 与其他文档的关系

| 来源 | 内容形态 | 关注点 |
|------|---------|--------|
| **本目录** | 轻量 ADR（标题 + 1-3 句，段落按需） | **完整决策追溯**（why） |
| [`../insights/`](../insights/) | 产品需求 / 用户问题 / 商业理由 | **为什么做这个产品** |
| [`../glossary.md`](../glossary.md) | 项目术语表（统一黑话） | **这个词指什么** |
| [`../../ARCHITECTURE.md`](../../ARCHITECTURE.md) | 技术架构 / 模块图 / 数据流 | **现在长什么样**（how） |
| [`../research/`](../research/) | 调研 / 可行性分析 | **有什么备选** |
| [`../exec-plans/`](../exec-plans/) | 执行计划 / 进度状态 | **现在在做什么** |

互为补充，不重复。本目录是关键决策的 source of truth。
